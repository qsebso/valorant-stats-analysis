# scheduling.rules
# How to tag each match's phase based on bracket stage.

Use the scraped `bracket_stage` value directly as the match's phase. No external lookup required.

If `bracket_stage` is ever empty or unrecognized, set `phase = "Unknown"` and log a WARNING for manual review.
